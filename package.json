{
  "name": "poying.dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/**/*",
    "serve": "parcel serve src/index.pug",
    "build": "parcel build --no-source-maps src/index.pug",
    "deploy": "rm -rf .cache dist && npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "staticFiles": {
    "staticPath": "public",
    "watcherGlob": "**"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "eslint-config-google": "^0.12.0",
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-static-files-copy": "^2.1.1",
    "pug": "^3.0.1",
    "purecss": "^1.0.0",
    "sass": "^1.18.0"
  },
  "dependencies": {}
}
